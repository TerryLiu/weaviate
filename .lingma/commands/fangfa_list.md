---
description: 列出该对象继承的接口和基类, 并列出公开的方法签名和简要含义;
---
### 1. 继承结构分析

*   **基类 (Base Classes)**:
    *   *(在此处列出直接父类，例如：`struct Name` 或 `class Name`)*
    *   *说明：Go 语言中通常通过组合（Embedding）实现类似继承的效果，请检查结构体中嵌入的其他结构体。*

*   **实现的接口 (Implemented Interfaces)**:
    *   *(在此处列出该对象实现的所有接口名称)*
    *   *说明：请检查该类型是否满足特定接口的所有方法签名。*

### 2. 列出公开的方法或函数的签名列表

| 方法名 | 签名 | 简要含义 |
| :--- | :--- | :--- |
| `MethodName` | `func (t Type) MethodName(args) returnTypes` | *(描述该方法的主要功能)* |
| ... | ... | ... |


